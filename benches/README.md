# Baseline benches (W1)

Report numbers; **no Mantle / µs SLO promises**.

Planned matrix (to be filled as Meta/UFS paths land):

1. Native UFS HEAD (OpenDAL local + S3)
2. External cold lookup / warm lookup
3. Path depth × directory size

Runners will live here or under `crates/*/benches` once the lookup path is wired end-to-end.
